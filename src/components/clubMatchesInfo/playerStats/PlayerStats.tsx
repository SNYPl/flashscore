import React from "react";
import style from "./style.module.css";

const PlayerStats = () => {
  return <article className={`${style.playerStats} `}></article>;
};

export default PlayerStats;
